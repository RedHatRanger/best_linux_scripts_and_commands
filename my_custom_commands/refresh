!#/usr/bin/env bash

# Save as /usr/local/bin/refresh - chmod 755
mybranch=$(whoami | cut -d "@" -f1 | cut -d "-" -f2)
echo $mybranch
git checkout main;
git pull;
git branch -D $mybranch;
git checkout -b $mybranch